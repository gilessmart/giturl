[project]
name = "giturl"
version = "0.1.0"
requires-python = ">=3.2"
dependencies = []

[project.scripts]
giturl = "giturl.cli:main"

[project.optional-dependencies]
dev = ["pytest", "pytest-cov"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
